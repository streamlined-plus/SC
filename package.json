{
  "name": "streamlinedcontent-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for streamlinedcontent.com, deployed to a Wix-managed headless project",
  "scripts": {
    "build": "./build.sh",
    "login": "wix login",
    "deploy": "./build.sh && wix release",
    "preview": "./build.sh && wix preview"
  },
  "devDependencies": {
    "@wix/cli": "^1.1.247"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
